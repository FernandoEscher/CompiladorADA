procedure x (x_d : in integer) is
begin
	if x = 4.4 and 10 > 9 and (x /= 8) and x = 4 then
	x := 4;
	x := 7;
	return 10;
		if x = 0 then
			loop
				x := 4;
			end loop;

			for I in 1..123 loop
				x := x / 5 * (5 + 2);
				while x < 4 loop
					for I in reverse 1..134 loop
					i := 5;
					end loop;	
				end loop;
			end loop;
			return 4;
		end if;
	elsif x = 4.5 and (x /= 9 and x < 9) then
 		return 98;
	else
		return 45;
	end if;
end x;
